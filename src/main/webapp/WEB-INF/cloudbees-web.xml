<?xml version="1.0"?>
<cloudbees-web-app xmlns="http://www.cloudbees.com/xml/webapp/1">
	<!-- Application ID (formatted CB_ACCOUNT/APPNAME) -->
	<appid>mortenk/fllplanner</appid>

	<!-- DataSources (use names refererenced via <resource-ref> in WEB-INF/web.xml) -->
	<resource name="jdbc/fllplanner" auth="Container" type="javax.sql.DataSource">
		<param name="username" value="fllplanner" />
		<param name="password" value="Knudsen1" />
		<param name="url" value="jdbc:cloudbees://fllplanner" />
	</resource>
</cloudbees-web-app>